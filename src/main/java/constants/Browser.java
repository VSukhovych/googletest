package constants;

public enum Browser {

    CHROME("Chrome"), FIREFOX("Firefox");

    private final String BROWSER_NAME;

    Browser(String BROWSER_NAME) {
        this.BROWSER_NAME = BROWSER_NAME;
    }
}
